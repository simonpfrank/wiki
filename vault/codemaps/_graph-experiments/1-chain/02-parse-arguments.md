# 2. Parse arguments

`cli.main` reads the workflow path, any `--env` overrides, the working
directory and the logging level, then configures logging.

Next: [Load the workflow](03-load-the-workflow.md)
