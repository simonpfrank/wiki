# 4. Validate

Three tiers, in order: `validate_static` (structure), `plan` (reasons across
all steps), and optionally `validate_existence` (opens real workbooks).

Nothing has been written yet — validation fails before anything is touched.

Next: [Execute the steps](05-execute-the-steps.md)
